---
name: prisma
description: >
  Full Prisma ORM documentation and skills. Use when writing Prisma schema, running CLI
  commands, Client API queries (CRUD, relations, transactions), database setup, upgrading
  to v7, Prisma Postgres, or implementing driver adapters. Read the appropriate reference
  for detailed guidance.
---

# Prisma ORM - Complete Reference

Unified skill covering Prisma schema, CLI, Client API, database setup, v7 upgrade, Prisma Postgres, and driver adapter implementation.

## When to Use

- **Schema / migrations / ORM API**: `references/prisma-orm-docs.md` — Full Prisma ORM v6.x reference (installation, schema, CRUD, filtering, transactions, raw queries)
- **CLI commands**: `references/prisma-cli/` — init, generate, migrate, db push/pull, studio, dev
- **Client API**: `references/prisma-client-api/` — constructor, model queries, filters, relations, transactions, raw queries
- **Database setup**: `references/prisma-database-setup/` — PostgreSQL, MySQL, SQLite, MongoDB, SQL Server, CockroachDB
- **Upgrade to v7**: `references/prisma-upgrade-v7/` — ESM, driver adapters, prisma.config.ts, removed features
- **Prisma Postgres**: `references/prisma-postgres/` — Console, create-db CLI, Management API, SDK
- **Driver adapter implementation**: `references/prisma-driver-adapter-implementation/` — Building SQL driver adapters for v7
